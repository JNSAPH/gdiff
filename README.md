# gdiff

A terminal UI for browsing a git repository's changes.

## Installation

Requires [Homebrew](https://brew.sh). The same command works on macOS and Linux:

```sh
brew install JNSAPH/taps/gdiff
```

Or build from source:

```sh
go install github.com/JNSAPH/gdiff@latest
```
